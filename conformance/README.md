# `.chems` conformance suite

This directory contains the executable conformance contract and will accumulate
the normative fixtures described by the
[language specification](../docs/chems-specification.md) and
[implementation plan](../docs/chems-implementation-plan.md).

## Authority

Fixtures test stable specification requirement IDs. Expected chemistry results
must be independently reviewed; they must not be generated by the engine under
test and then accepted as their own oracle.

## Layout

```text
conformance/
  manifest.json
  manifest.schema.json
  requirements.json
  requirements.schema.json
  reserved-words.txt
  specification/
  encoding-layout/
  parsing/
  formatting/
  quantities-types/
  formula-species/
  materials/
  procedures/
  claims-holes/
  kernel-tactics/
  catalogue/
  diagnostics-tooling/
  artifacts/
  end-to-end/
```

Each case names requirement IDs owned by its component, its expected primary
result, structured diagnostic codes/severities/exact byte spans, and any domain
input/output, source, catalogue, canonical formatted source, AST, HIR,
derivation, or artifact fixtures it consumes.

Fixture names equal the lowercase-kebab case ID plus a three-digit sequence and
lowercase extension, for example `tab-indentation-001.chems` and
`silver-chloride-001.artifact.json`. Every fixture is checked in directly under
the component that owns its case; canonical path validation rejects traversal
and symlinks that resolve outside that directory.

## Commands

```sh
cargo run -p chems-conformance -- validate
cargo run -p chems-conformance -- report
```

`validate` succeeds when the specification, registry, grammar, language data,
schemas, manifest, and referenced paths are internally coherent. `report`
prints per-component coverage and exits with status 3 until all requirements
have conformance cases. An empty suite therefore cannot silently appear green.

Slice 1 checks in six executable domain cases covering exact decimal
precision, every registered unit and affine temperature behavior, authored
quantity conversions with exact derivations, grouped and adduct formula
normalization, charge/phase values, and canonical serialization. The
`chem-domain` integration suite evaluates these inputs and byte-compares its
canonical output with the independently authored, canonical `.domain.json`
files.
